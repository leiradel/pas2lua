return {
  tform = {
    type = 'tform',
    fields = {
      top = {
        type = 'integer'
      },
      left = {
        type = 'integer'
      },
      height = {
        type = 'integer'
      },
      clientwidth = {
        type = 'integer'
      },
      clientheight = {
        type = 'integer'
      },
      doublebuffered = {
        type = 'boolean'
      },
      show = {
        type = 'procedure'
      },
      bordericons = {
        type = 'set'
      },
      borderstyle = {
        type = 'integer'
      },
      color = {
        type = 'integer'
      },
      transparentcolor = {
        type = 'boolean'
      },
      transparentcolorvalue = {
        type = 'integer'
      },
      keypreview = {
        type = 'boolean'
      },
      oldcreateorder = {
        type = 'boolean'
      },
      popupmenu = {
        type = 'tform'
      },
      printscale = {
        type = 'integer'
      },
      scaled = {
        type = 'boolean'
      },
      onactivate = {
        type = 'procedure'
      },
      onclose = {
        type = 'procedure'
      },
      oncreate = {
        type = 'procedure'
      },
      onkeydown = {
        type = 'procedure'
      },
      onkeyup = {
        type = 'procedure'
      },
      pixelsperinch = {
        type = 'integer'
      },
      textheight = {
        type = 'integer'
      },
      font = {
        type = 'tfont'
      }
    }
  },
  tcloseaction = {
    type = 'tcloseaction',
    fields = {
    }
  },
  cafree = {
    type = 'integer'
  },
  bsnone = {
    type = 'integer'
  },
  ponone = {
    type = 'integer'
  },
  bisystemmenu = {
    type = 'integer'
  },
  vk_down = {
    -- not sure if this should be in the forms unit
    type = 'integer'
  },
  vk_up = {
    -- not sure if this should be in the forms unit
    type = 'integer'
  },
  vk_left = {
    -- not sure if this should be in the forms unit
    type = 'integer'
  },
  vk_right = {
    -- not sure if this should be in the forms unit
    type = 'integer'
  },
  vk_control = {
    -- not sure if this should be in the forms unit
    type = 'integer'
  },
  vk_menu = {
    -- not sure if this should be in the forms unit
    type = 'integer'
  },
  vk_shift = {
    -- not sure if this should be in the forms unit
    type = 'integer'
  },
  vk_insert = {
    -- not sure if this should be in the forms unit
    type = 'integer'
  }
}
