return {
  pfsoundsample = {
    -- not sure if this should be in the fmodtypes unit
    type = 'pfsoundsample',
    fields = {
    }
  },
  fsound_free = {
    -- not sure if this should be in the fmodtypes unit
    type = 'integer'
  },
  fsound_all = {
    -- not sure if this should be in the fmodtypes unit
    type = 'integer'
  }
}
